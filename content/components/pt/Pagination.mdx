---
title: Pagination Paginação
hydration: 2
category: Data Display
---

import { Pagination } from "../../../app/components/ui";
import { DemoPreview } from "../../../app/components/demo-preview";

Um componente para navegar entre várias páginas de conteúdo.

## Uso

<DemoPreview>
  <Pagination count={100} pageSize={10} />
</DemoPreview>

### Construtor de páginas do CMS

Este componente está disponível como um bloco `pagination` no [Construtor de Páginas](/docs/PageBuilder) (`content/pages/*.json`):

```json
{
  "type": "pagination",
  "count": 100,
  "pageSize": 10
}
```

## Propriedades

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `count` | `number` | - | Número total de itens. |
| `pageSize` | `number` | `10` | Número de itens por página. |
| `defaultPage` | `number` | `1` | A página inicial a exibir. |

| `type` | \`'button' \ | 'link'\` | `'button'` | Se deve usar botões ou links para a navegação. |

| `interactive` | `boolean` | `true` | Se deve habilitar a interatividade do lado do cliente. |

## Configuração do CMS

```yaml
- label: Pagination
  name: pagination
  widget: object
  fields:
    - { label: Count, name: count, widget: number }
    - { label: Page Size, name: pageSize, widget: number, default: 10 }
    - { label: Default Page, name: defaultPage, widget: number, default: 1 }
    - label: Type
      name: type
      widget: select
      options: [button, link]
      default: button
```

## Hidratação

Nível 2: se hidrata quando `onPageChange` é fornecido, ou quando não está em modo link com `getPageUrl`.
